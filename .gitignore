# Dependencies
node_modules
.pnpm-store

# Build output
dist
build
target

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Tooling caches
.turbo
.cache
*.tsbuildinfo

# Server runtime state — do NOT commit user data, hashed tokens, or
# anything the server writes during operation.
apps/server/cognigram.redb
apps/server/cognigram.redb.*
apps/server/data/uploads/avatars/
apps/server/data/uploads/channel-avatars/
apps/server/data/uploads/files/
# Keep apps/server/data/uploads/stickers/default/ — that's the bundled
# default sticker pack and is part of the codebase.
